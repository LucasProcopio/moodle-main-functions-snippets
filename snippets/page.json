{
  "Moodle page structure": {
    "prefix": ["mdl_page", "mpage"],
    "body": [
      "require_once(__DIR__.'/../../config.php');\n",
      "\\$plugin_name = 'type_pluginname';\n",
      "require_login();\n",
      "\\$PAGE->set_url(\\$CFG->wwwroot.'/path/pagefile.php');\n",
      "\\$context = context_system::instance();",
      "\\$PAGE->set_context(\\$context);\n",
      "\\$PAGE->set_title(get_string('string', \\$plugin_name));",
      "\\$PAGE->set_heading(get_string('string', \\$plugin_name));",
      "\\$PAGE->set_pagelayout('admin');\n",
      "echo \\$OUTPUT->header();\n\n",
      "// your code here ...\n\n",
      "echo \\$OUTPUT->footer();"
    ],
    "description": "Add a basic moodle web page"
  }
}
